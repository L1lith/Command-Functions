const cmd = require('../../cmd')
const { ENTER, create } = cmd

//why is this test broken :(
// describe('Ask me about my day CLI', () => {
//   it('should return different input depending on what response was provided', async () => {
//     const { execute } = create('tests/library2')
//     const response = await execute(['askAboutMyDay'], ['yes', ENTER], { timeout: 300 })
//   })
// })
